import { Form } from './components';

const Home = () => {
  return (
    <main className='flex min-h-screen flex-col items-center justify-center bg-bgColor px-[24px] py-[96px] md:py-[120px]'>
      <Form />
    </main>
  );
};

export default Home;
